<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Hst.Imager.AvaloniaApp.ViewModels"
             xmlns:views="using:Hst.Imager.AvaloniaApp.Views"
             xmlns:ia="clr-namespace:Projektanker.Icons.Avalonia;assembly=Projektanker.Icons.Avalonia"
             x:Class="Hst.Imager.AvaloniaApp.Views.InfoView"
             x:DataType="vm:InfoViewModel">

  <ScrollViewer>
    <StackPanel Spacing="12" Margin="0,8">

      <StackPanel Spacing="8" MaxWidth="700" HorizontalAlignment="Left">
        <views:ElevationBanner />

        <TextBlock Classes="page-title" Text="Info" />
        <TextBlock Classes="page-description"
                   Text="Display information about an image file or a physical disk." />

        <Border Background="{DynamicResource AppErrorBackgroundBrush}" CornerRadius="4" Padding="8"
                IsVisible="{Binding HasError}">
          <TextBlock Classes="error-text" Text="{Binding ErrorMessage}" TextWrapping="Wrap" />
        </Border>

        <!-- Source -->
        <StackPanel Orientation="Horizontal" Spacing="5">
          <ia:Icon Value="fa-upload" Classes="field-icon" />
          <TextBlock Classes="field-label" Text="Source" />
        </StackPanel>
        <ComboBox ItemsSource="{Binding SourceTypeOptions}"
                  SelectedItem="{Binding SourceType}"
                  DisplayMemberBinding="{Binding Title}"
                  HorizontalAlignment="Stretch" />

        <!-- Source: image file -->
        <StackPanel Spacing="4" IsVisible="{Binding IsImageFile}">
          <StackPanel Orientation="Horizontal" Spacing="5">
            <ia:Icon Value="fa-file" Classes="field-icon" />
            <TextBlock Classes="field-label" Text="Image file" />
          </StackPanel>
          <Grid ColumnDefinitions="*,Auto">
            <TextBox Grid.Column="0" Text="{Binding ImagePath}" />
            <Button Grid.Column="1" Margin="8,0,0,0" Command="{Binding BrowsePathCommand}" ToolTip.Tip="Browse">
              <ia:Icon Value="fa-folder-open" />
            </Button>
          </Grid>
        </StackPanel>

        <!-- Source: physical disk -->
        <StackPanel Spacing="4" IsVisible="{Binding IsPhysicalDisk}">
          <StackPanel Orientation="Horizontal" Spacing="5">
            <ia:Icon Value="fa-hdd" Classes="field-icon" />
            <TextBlock Classes="field-label" Text="Physical disk" />
          </StackPanel>
          <Grid ColumnDefinitions="*,Auto">
            <ComboBox Grid.Column="0"
                      ItemsSource="{Binding MediaItems}"
                      SelectedItem="{Binding SelectedMedia}"
                      DisplayMemberBinding="{Binding Label}"
                      HorizontalAlignment="Stretch" />
            <Button Grid.Column="1" Margin="8,0,0,0" Command="{Binding RefreshMediaCommand}" ToolTip.Tip="Refresh">
              <ia:Icon Value="fa-sync-alt" />
            </Button>
          </Grid>
        </StackPanel>

        <!-- PiStorm disk -->
        <StackPanel Spacing="4" IsVisible="{Binding HasPiStormDisks}">
          <StackPanel Orientation="Horizontal" Spacing="5">
            <ia:Icon Value="fa-chart-pie" Classes="field-icon" />
            <TextBlock Classes="field-label" Text="PiStorm disk to read" />
          </StackPanel>
          <ComboBox ItemsSource="{Binding PiStormDiskOptions}"
                    SelectedItem="{Binding SelectedPiStormDisk}"
                    DisplayMemberBinding="{Binding Title}"
                    PlaceholderText="None available"
                    HorizontalAlignment="Stretch" />
        </StackPanel>

        <!-- Advanced -->
        <Expander IsExpanded="False" HorizontalAlignment="Stretch">
          <Expander.Header>
            <StackPanel Orientation="Horizontal" Spacing="6">
              <ia:Icon Value="fa-gear" />
              <TextBlock Text="Advanced" />
            </StackPanel>
          </Expander.Header>
          <StackPanel Spacing="8" Margin="0,4">
            <CheckBox Content="Byteswap sectors" IsChecked="{Binding Byteswap}" />
          </StackPanel>
        </Expander>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" Margin="0,8,0,0">
          <Button Command="{Binding CancelCommand}">
            <StackPanel Orientation="Horizontal" Spacing="6">
              <ia:Icon Value="fa-ban" />
              <TextBlock Text="Cancel" />
            </StackPanel>
          </Button>
          <Button Command="{Binding GetInfoCommand}" Classes="accent">
            <StackPanel Orientation="Horizontal" Spacing="6">
              <ia:Icon Value="fa-info" />
              <TextBlock Text="Get info" />
            </StackPanel>
          </Button>
        </StackPanel>
      </StackPanel>

      <!-- Loading indicator -->
      <ProgressBar IsIndeterminate="True" IsVisible="{Binding IsLoading}" Height="4" />

      <StackPanel Spacing="4" Margin="0,8,0,0" IsVisible="{Binding HasDiskInfo}">
        <TextBlock Classes="section-title" Margin="0" Text="{Binding SourceTypeFormatted, StringFormat='Source {0}'}" />
        <TextBlock Foreground="{DynamicResource AppBodyForegroundBrush}" Text="{Binding SourceTypeFormatted, StringFormat='Disk information read from source {0}.'}" />
      </StackPanel>

      <!-- Tabbed disk info -->
      <TabControl IsVisible="{Binding HasDiskInfo}">

        <!-- Overview tab -->
        <TabItem Header="Overview">
          <StackPanel Spacing="8" Margin="0,8">
            <CheckBox Content="Show unallocated" IsChecked="{Binding ShowUnallocated}" />

            <ItemsControl ItemsSource="{Binding OverviewSections}">
              <ItemsControl.ItemTemplate>
                <DataTemplate DataType="vm:OverviewSectionViewModel">
                  <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                    <DataGrid ItemsSource="{Binding Parts}"
                              IsReadOnly="True" AutoGenerateColumns="False" CanUserSortColumns="False"
                              GridLinesVisibility="All" Background="{DynamicResource AppPanelBackgroundBrush}" Margin="0,4">
                      <DataGrid.Columns>
                        <DataGridTextColumn Header="Type" Binding="{Binding TypeDisplay}" Width="140" />
                        <DataGridTextColumn Header="File system" Binding="{Binding FileSystem}" Width="100" />
                        <DataGridTextColumn Header="#" Binding="{Binding Number}" Width="40" />
                        <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="90" />
                        <DataGridTextColumn Header="Start offset" Binding="{Binding StartOffset}" Width="90" />
                        <DataGridTextColumn Header="End offset" Binding="{Binding EndOffset}" Width="90" />
                        <DataGridTextColumn Header="Start" Binding="{Binding StartSecOrCyl}" Width="80" />
                        <DataGridTextColumn Header="End" Binding="{Binding EndSecOrCyl}" Width="80" />
                      </DataGrid.Columns>
                    </DataGrid>
                  </Expander>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </TabItem>

        <!-- Details tab -->
        <TabItem Header="Details">
          <StackPanel Spacing="8" Margin="0,8">
            <CheckBox Content="Show human readable sizes" IsChecked="{Binding ShowHumanReadable}" />

            <ItemsControl ItemsSource="{Binding DetailSections}" x:CompileBindings="False">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <ContentControl Content="{Binding}">
                    <ContentControl.DataTemplates>

                      <DataTemplate DataType="vm:DiskInfoDetailSection">
                        <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                          <Border Background="{DynamicResource AppPanelBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4">
                            <Grid ColumnDefinitions="160,*" RowDefinitions="Auto,Auto,Auto,Auto">
                              <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Name}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="1" Grid.Column="0" Text="Path" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path}" Foreground="{DynamicResource AppStrongForegroundBrush}" TextWrapping="Wrap" />
                              <TextBlock Grid.Row="2" Grid.Column="0" Text="Size" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Size}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="3" Grid.Column="0" Text="Sparse file size" Foreground="{DynamicResource AppSecondaryForegroundBrush}"
                                         IsVisible="{Binding IsSparseFile}" />
                              <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SparseFileSize}" Foreground="{DynamicResource AppStrongForegroundBrush}"
                                         IsVisible="{Binding IsSparseFile}" />
                            </Grid>
                          </Border>
                        </Expander>
                      </DataTemplate>

                      <DataTemplate DataType="vm:GeometryDetailSection">
                        <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                          <Border Background="{DynamicResource AppPanelBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4">
                            <Grid ColumnDefinitions="160,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto">
                              <TextBlock Grid.Row="0" Grid.Column="0" Text="Size" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Capacity}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="1" Grid.Column="0" Text="Sector size" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SectorSize}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="2" Grid.Column="0" Text="Total sectors" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TotalSectors}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="3" Grid.Column="0" Text="Cylinders" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Cylinders}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="4" Grid.Column="0" Text="Heads per cyl" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding HeadsPerCylinder}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="5" Grid.Column="0" Text="Sectors per track" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SectorsPerTrack}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                            </Grid>
                          </Border>
                        </Expander>
                      </DataTemplate>

                      <DataTemplate DataType="vm:MbrPartitionsDetailSection">
                        <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                          <DataGrid ItemsSource="{Binding Rows}"
                                    IsReadOnly="True" AutoGenerateColumns="False" CanUserSortColumns="False"
                                    GridLinesVisibility="All" Background="{DynamicResource AppPanelBackgroundBrush}" Margin="0,4">
                            <DataGrid.Columns>
                              <DataGridTextColumn Header="Number" Binding="{Binding Number}" />
                              <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
                              <DataGridTextColumn Header="Type" Binding="{Binding Type}" />
                              <DataGridTextColumn Header="File system" Binding="{Binding FileSystem}" />
                              <DataGridTextColumn Header="Size" Binding="{Binding Size}" />
                              <DataGridTextColumn Header="Start sector" Binding="{Binding StartSector}" />
                              <DataGridTextColumn Header="End sector" Binding="{Binding EndSector}" />
                              <DataGridTextColumn Header="Active" Binding="{Binding Active}" />
                              <DataGridTextColumn Header="Primary" Binding="{Binding Primary}" />
                            </DataGrid.Columns>
                          </DataGrid>
                        </Expander>
                      </DataTemplate>

                      <DataTemplate DataType="vm:GptPartitionsDetailSection">
                        <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                          <DataGrid ItemsSource="{Binding Rows}"
                                    IsReadOnly="True" AutoGenerateColumns="False" CanUserSortColumns="False"
                                    GridLinesVisibility="All" Background="{DynamicResource AppPanelBackgroundBrush}" Margin="0,4">
                            <DataGrid.Columns>
                              <DataGridTextColumn Header="Number" Binding="{Binding Number}" />
                              <DataGridTextColumn Header="GUID" Binding="{Binding Guid}" />
                              <DataGridTextColumn Header="Type" Binding="{Binding Type}" />
                              <DataGridTextColumn Header="File system" Binding="{Binding FileSystem}" />
                              <DataGridTextColumn Header="Size" Binding="{Binding Size}" />
                              <DataGridTextColumn Header="Start sector" Binding="{Binding StartSector}" />
                              <DataGridTextColumn Header="End sector" Binding="{Binding EndSector}" />
                            </DataGrid.Columns>
                          </DataGrid>
                        </Expander>
                      </DataTemplate>

                      <DataTemplate DataType="vm:RdbInfoDetailSection">
                        <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                          <Border Background="{DynamicResource AppPanelBackgroundBrush}" CornerRadius="4" Padding="8" Margin="0,4">
                            <Grid ColumnDefinitions="160,*" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto">
                              <TextBlock Grid.Row="0"  Grid.Column="0" Text="Product"        Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="0"  Grid.Column="1" Text="{Binding Product}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="1"  Grid.Column="0" Text="Vendor"         Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="1"  Grid.Column="1" Text="{Binding Vendor}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="2"  Grid.Column="0" Text="Revision"       Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="2"  Grid.Column="1" Text="{Binding Revision}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="3"  Grid.Column="0" Text="Size"           Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="3"  Grid.Column="1" Text="{Binding Size}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="4"  Grid.Column="0" Text="Cylinders"      Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="4"  Grid.Column="1" Text="{Binding Cylinders}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="5"  Grid.Column="0" Text="Heads"          Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="5"  Grid.Column="1" Text="{Binding Heads}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="6"  Grid.Column="0" Text="Sectors"        Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="6"  Grid.Column="1" Text="{Binding Sectors}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="7"  Grid.Column="0" Text="Block size"     Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="7"  Grid.Column="1" Text="{Binding BlockSize}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="8"  Grid.Column="0" Text="Start cylinder" Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="8"  Grid.Column="1" Text="{Binding StartCylinder}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="9"  Grid.Column="0" Text="End cylinder"   Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="9"  Grid.Column="1" Text="{Binding EndCylinder}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="10" Grid.Column="0" Text="Flags"          Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="10" Grid.Column="1" Text="{Binding Flags}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="11" Grid.Column="0" Text="Host id"        Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="11" Grid.Column="1" Text="{Binding HostId}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="12" Grid.Column="0" Text="RDB block lo"   Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="12" Grid.Column="1" Text="{Binding RdbBlockLo}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                              <TextBlock Grid.Row="13" Grid.Column="0" Text="RDB block hi"   Foreground="{DynamicResource AppSecondaryForegroundBrush}" />
                              <TextBlock Grid.Row="13" Grid.Column="1" Text="{Binding RdbBlockHi}" Foreground="{DynamicResource AppStrongForegroundBrush}" />
                            </Grid>
                          </Border>
                        </Expander>
                      </DataTemplate>

                      <DataTemplate DataType="vm:RdbFileSystemsDetailSection">
                        <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                          <DataGrid ItemsSource="{Binding Rows}"
                                    IsReadOnly="True" AutoGenerateColumns="False" CanUserSortColumns="False"
                                    GridLinesVisibility="All" Background="{DynamicResource AppPanelBackgroundBrush}" Margin="0,4">
                            <DataGrid.Columns>
                              <DataGridTextColumn Header="Number" Binding="{Binding Number}" />
                              <DataGridTextColumn Header="DOS type" Binding="{Binding DosType}" />
                              <DataGridTextColumn Header="Version" Binding="{Binding Version}" />
                              <DataGridTextColumn Header="File system name" Binding="{Binding FileSystemName}" />
                              <DataGridTextColumn Header="Size" Binding="{Binding Size}" />
                            </DataGrid.Columns>
                          </DataGrid>
                        </Expander>
                      </DataTemplate>

                      <DataTemplate DataType="vm:RdbPartitionsDetailSection">
                        <Expander Header="{Binding Title}" IsExpanded="{Binding IsExpanded}" Margin="0,4">
                          <ScrollViewer HorizontalScrollBarVisibility="Auto">
                            <DataGrid ItemsSource="{Binding Rows}"
                                      IsReadOnly="True" AutoGenerateColumns="False" CanUserSortColumns="False"
                                      GridLinesVisibility="All" Background="{DynamicResource AppPanelBackgroundBrush}" Margin="0,4">
                              <DataGrid.Columns>
                                <DataGridTextColumn Header="Number" Binding="{Binding Number}" />
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                                <DataGridTextColumn Header="Size" Binding="{Binding Size}" />
                                <DataGridTextColumn Header="Start cyl" Binding="{Binding StartCyl}" />
                                <DataGridTextColumn Header="End cyl" Binding="{Binding EndCyl}" />
                                <DataGridTextColumn Header="Total cyl" Binding="{Binding TotalCyl}" />
                                <DataGridTextColumn Header="Heads" Binding="{Binding Heads}" />
                                <DataGridTextColumn Header="Blks/track" Binding="{Binding BlocksPerTrack}" />
                                <DataGridTextColumn Header="Buffers" Binding="{Binding Buffers}" />
                                <DataGridTextColumn Header="FS block" Binding="{Binding FsBlockSize}" />
                                <DataGridTextColumn Header="Reserved" Binding="{Binding Reserved}" />
                                <DataGridTextColumn Header="PreAlloc" Binding="{Binding PreAlloc}" />
                                <DataGridTextColumn Header="Bootable" Binding="{Binding Bootable}" />
                                <DataGridTextColumn Header="Boot pri" Binding="{Binding BootPriority}" />
                                <DataGridTextColumn Header="No mount" Binding="{Binding NoMount}" />
                                <DataGridTextColumn Header="DOS type" Binding="{Binding DosType}" />
                                <DataGridTextColumn Header="Mask" Binding="{Binding Mask}" />
                                <DataGridTextColumn Header="Max xfer" Binding="{Binding MaxTransfer}" />
                              </DataGrid.Columns>
                            </DataGrid>
                          </ScrollViewer>
                        </Expander>
                      </DataTemplate>

                    </ContentControl.DataTemplates>
                  </ContentControl>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </StackPanel>
        </TabItem>

      </TabControl>

    </StackPanel>
  </ScrollViewer>
</UserControl>
